title = "gitleaks config"

[[rules]]
id = "azure-client-secret"
regex = '''secret = (.+)'''
[rules.verify]
url = "https://example.com"
headers = {"X-Client-ID" = "${azure-client-id}", Authorization = "Bearer ${azure-client-secret}"}
expectedStatus = [200]
