title = "gitleaks config"

[[rules]]
id = "azure-client-secret"
regex = '''secret = (.+)'''
[rules.verify]
httpVerb = "GET"
url = "https://example.com"
headers = {"Authorization" = "${azure-client-id}"}
expectedStatus = [200]
